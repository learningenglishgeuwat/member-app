@{
  # PowerShell script to convert and optimize images
  # Using FFMPEG which is more reliable cross-platform
  
  "Kepala.png" = @{
    quality = 75
    sizes = @(56, 112, 48, 96, 40, 80)
  }
  "Kepala1.png" = @{
    quality = 75
    sizes = @(48, 96, 40, 80)
  }
}
